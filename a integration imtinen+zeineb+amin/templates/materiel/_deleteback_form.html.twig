<form method="post" action="{{ path('app_materiel_deleteback', {'idm': materiel.idm}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ materiel.idm) }}">
    <button class="btn">Delete</button>
</form>
