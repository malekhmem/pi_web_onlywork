{% form_theme form 'bootstrap_5_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
<button  class="btn btn-primary btn-sm"  style="background-color: #87CEFA; border-color :#87CEFA;">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
