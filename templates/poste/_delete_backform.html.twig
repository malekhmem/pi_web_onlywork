<form method="post" action="{{ path('app_poste_deleteback', {'idp': poste.idp}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('deleteback' ~ poste.idp) }}">
    <button class="btn">Delete</button>
</form>