<form method="post" action="{{ path('app_poste_delete', {'idp': poste.idp}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ poste.idp) }}">
    <button class="btn">Delete</button>
</form>
