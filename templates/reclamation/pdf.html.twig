<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Reclamation</title>
 <style>
  

     body {
    background-image: url('file:///C:/tester.png');
    background-repeat: no-repeat;
    background-size: cover;
    font-family: 'Open Sans', sans-serif;
    }
        
        h1 {
            color: #00f;
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
        }

        table {
            border-collapse: collapse;
            width: 100%;
        }

        th {
            background-color: #00f;
            border: 1px solid #fff;
            padding: 10px;
            text-align: center;
              color: white; /* Add this line to set text color to white */
        }

        td {
            border: 1px solid #fff;
            padding: 10px;
            text-align: center;
              color: white; /* Add this line to set text color to white */
        }

        tbody tr:nth-child(even) {
            background-color: #000;
        }

        tbody tr:nth-child(odd) {
            background-color: #444;
        }
    </style>



</head>
<body>
    <h1>Reclamation</h1>
 <table class="table">
    <thead>
        <tr>
            <th>Idr</th>
            <th>Descr</th>
            <th>Nomr</th>
            <th>Emailr</th>
            <th>Type</th>
        </tr>
    </thead>
    <tbody>
        {% for row in data %}
        <tr>
            <td>{{ row.idr }}</td>
            <td>{{ row.descr }}</td>
            <td>{{ row.nomr }}</td>
            <td>{{ row.emailr }}</td>
            <td>{{ row.type }}</td>
        </tr>
        {% endfor %}
    </tbody>
</table>

</body>
</html>
