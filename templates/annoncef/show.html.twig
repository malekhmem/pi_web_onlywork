{% extends 'front.html.twig' %}

{% block title %}Annoncef{% endblock %}

{% block body %}
    <h1>Annoncef</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nomf</th>
                <td>{{ annoncef.nomf }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ annoncef.adresse }}</td>
            </tr>
            <tr>
                <th>Emailf</th>
                <td>{{ annoncef.emailf }}</td>
            </tr>
            <tr>
                <th>Descf</th>
                <td>{{ annoncef.descf }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_annoncef_index') }}">back to list</a>

    <a href="{{ path('app_annoncef_edit', {'idf': annoncef.idf}) }}">edit</a>
    <button style="width:15% ;height:40px;color:white">
			<a href="{{ path('generator_servicee' , {'idf': annoncef.idf})}}" >
				Imprimer en pdf
			</a>
		</button>

    {{ include('annoncef/_delete_form.html.twig') }}
{% endblock %}
